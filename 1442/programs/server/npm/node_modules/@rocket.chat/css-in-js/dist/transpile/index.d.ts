import { Options } from '@emotion/stylis';
import { LogicalPropertiesOptions } from './logicalProperties';
declare type TranspileOptions = LogicalPropertiesOptions & Options;
/**
 * Transpiles CSS Modules content to CSS rules.
 */
export declare const transpile: (selector: string, content: string, options?: TranspileOptions | undefined) => string;
export {};
