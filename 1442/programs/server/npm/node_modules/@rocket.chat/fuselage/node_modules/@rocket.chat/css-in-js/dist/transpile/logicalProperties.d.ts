import { Plugin } from '@emotion/stylis';
export declare type LogicalPropertiesOptions = {
    supportedProperties?: string[];
};
export declare const createLogicalPropertiesPlugin: (options: LogicalPropertiesOptions) => Plugin;
